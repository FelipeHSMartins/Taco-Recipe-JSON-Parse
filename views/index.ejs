<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="0001" name="choice">🍗</button>
    <button type="submit" value="0002" name="choice">🥩</button>
    <button type="submit" value="0003" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">

    <!-- If no recipe yet, show: -->
    <% if (!locals.recipe) {  %>
      <h2>Pick your favourite taco ingredient 👆</h2>
    <% } else { %>
      <h2><%= recipe["name"] %></h2>
      <h3>Ingredients:</h3>
    <ul id="ingredientsList">
      <li><%= recipe["ingredients"]["protein"]["name"] %>, <%= recipe["ingredients"]["protein"]["preparation"] %></li>
      <li><%= recipe["ingredients"]["salsa"]["name"] %></li>
      <li>Every other topping</li>
      <% recipe["ingredients"]["toppings"].forEach((e) => { %>
        <li><%= e.quantity %> of <%= e.name %></li>
      <% })  %> 
    </ul>
    <% }  %>
    <!-- Else, show the recipe name e.g. -->

    



  </div>
</body>

</html>